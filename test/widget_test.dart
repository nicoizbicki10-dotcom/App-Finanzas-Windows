import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    // App requires Hive initialization — tests run separately
    expect(true, isTrue);
  });
}
